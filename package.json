{
  "name": "angular-client-example",
  "version": "1.0.0",
  "description": "Cliente para exemplo em angular",
  "repository": "https://github.com/guilhermewm/angular-client-example",
  "license": "ISC",
  "devDependencies": {
    "bower": "^1.7.7",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "http-server ./app -a localhost -p 8000 -c-1",
    "pretest": "npm install"
  },
  "dependencies": {
    "angular-ui-router": "^0.4.2"
  }
}
